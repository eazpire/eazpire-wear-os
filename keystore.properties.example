# Copy to keystore.properties (not committed) and set paths/passwords.
# CI writes keystore.properties from GitHub Secrets (google-play.yml).
# Same upload keystore as eazpire-android / eazpire-creator-watch.

storeFile=release.keystore
storePassword=CHANGE_ME
keyAlias=CHANGE_ME
keyPassword=CHANGE_ME
